import 'package:flutter/material.dart';

class HtmlLearningWebViewPlatformComponent extends StatelessWidget {
  const HtmlLearningWebViewPlatformComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Unsupported Platform'));
  }
}
