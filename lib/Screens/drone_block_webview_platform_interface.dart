import 'package:flutter/material.dart';

class DroneBlockWebViewPlatformComponent extends StatelessWidget {
  const DroneBlockWebViewPlatformComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Unsupported Platform'));
  }
}
