import 'package:flutter/material.dart';

class PowerPointNoAiWebViewPlatformComponent extends StatelessWidget {
  const PowerPointNoAiWebViewPlatformComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Unsupported Platform'));
  }
}
