import 'package:flutter/material.dart';

class PowerPointAppWebViewPlatformComponent extends StatelessWidget {
  const PowerPointAppWebViewPlatformComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Unsupported Platform'));
  }
}
