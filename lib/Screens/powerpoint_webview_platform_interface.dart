import 'package:flutter/material.dart';

class PowerPointWebViewPlatformComponent extends StatelessWidget {
  const PowerPointWebViewPlatformComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Unsupported Platform'));
  }
}
